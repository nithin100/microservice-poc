spring:
  application:
    name: addition-service
    
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: configclient
      password: configpassword
      
      